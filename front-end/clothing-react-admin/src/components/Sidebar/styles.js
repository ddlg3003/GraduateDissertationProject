import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  root: {
    width: '240px',
    zIndex: 1,
  },
  logo: {
    display: 'flex',
    justifyContent: 'center',
    padding: '8% 20%',
  },
}));
